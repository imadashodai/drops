<h1>Users#index</h1>
<div>
<% @users.each do |user| %>
  <p><%= user.name %>
    <span><%= link_to '編集', edit_user_path(user.id)%></span>
    <span><%= button_to '削除', {action: :destroy, id: user.id}, method: :delete %></span>
  </p>
<% end %>
</div>
